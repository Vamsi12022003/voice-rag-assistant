version: "3.9"

services:
  voice-rag:
    build: .
    ports:
      - "8000:8000"
    volumes:
      - ./data:/app/data
      - ./vectorstore:/app/vectorstore
      - ./audio_uploads:/app/audio_uploads
    env_file:
      - .env
    restart: unless-stopped
